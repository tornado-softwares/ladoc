---
title: Code Block 
description : Learn the Markdown syntax about code blocks.
---

````ts twoslash title="Test" lineNumbers
type Player = {
  /**
   * The player name
   * ```php
   * $a = "60"
   * ```
   *
   * @default 'user'
   */
  name: string;
};

// ---cut---
// @noErrors
console.g;
//       ^|

const player: Player = { name: 'Hello World' };
//    ^?
````

```ts twoslash LineNumbers
const a = '123';

console.log(a);
//      ^^^
```

```ts twoslash
// @errors: 2588
const a = '123';

a = 132;
```


Code blocks are usefull to show the reader some code examples, with ladoc it's super easy !

```py
a = 0
if (a < 0):
    print("Byebye World")
else:
    print("Hello World")
```

And look `This is inlined`


```ts title="src/index.ts"
console.log("Hello");
```

```ts {1,3}
const a = 1;
const b = 2;
console.log(a + b);
```

```ts {2} ins={3} del={1}
const oldValue = 1;
const value = 2;
console.log(value);
```
 Heehee
```ts
// [!code focus]
const a = 1;

// [!code highlight]
const b = 2;

// [!code ++]
const added = true;

// [!code --]
const removed = false;

// [!code error]
throw new Error("Oops");

// [!code warning]
console.warn("Warning");
```

```ts
const a = 1;
// [!code highlight:2]
const b = 2;
const c = 3;
```

```ts
// [!code focus:4]
const a = 1;
const b = 2;
const c = 3;
const d = 4;
```

```ts
// [!code ++:2]
const a = 1;
const b = 2;
```

```ts
// [!code --:2]
const a = 1;
const b = 2;
```

```ts
// [!code error:2]
const a = 1;
throw new Error("Boom");
```

```ts
// [!code warning:2]
const a = 1;
console.warn("Deprecated");
```
